{
  "name": "vue3-media-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter vue3-media build",
    "build:all": "pnpm -r build",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
